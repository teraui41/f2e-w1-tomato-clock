import React from 'react';
import styled from 'styled-components';

class Menu extends React.PureComponent {
    render() {
        return (
            <div></div>
        )
    }
}

export default Menu;
